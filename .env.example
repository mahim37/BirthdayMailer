# --- Google Sheet Configuration ---
# The exact name of your Google Sheet file
SHEET_FILE_NAME="Birthday List"
# The exact name of the worksheet (tab) within the Sheet file
SHEET_NAME="Sheet1"
# The row number (1-based) containing your column headers
HEADER_ROW=1
# Exact header text for the column containing names
NAME_COLUMN_HEADER="Name"
# Exact header text for the column containing birthdays
BIRTHDAY_COLUMN_HEADER="Birthday"
# Exact header text for the column containing email addresses
EMAIL_COLUMN_HEADER="Emails"

# --- Email Sender Configuration ---
# The email address the birthday emails will be sent FROM
SENDER_EMAIL="your_email@gmail.com"
# SMTP Username (often the same as SENDER_EMAIL for Gmail)
SMTP_USERNAME="your_email@gmail.com"
# SMTP Password (for Gmail, use an App Password: https://myaccount.google.com/apppasswords)
SMTP_PASSWORD="your_gmail_app_password"
# SMTP Server address (default is for Gmail)
# SMTP_SERVER="smtp.gmail.com"
# SMTP Port (default is 587 for TLS)
# SMTP_PORT=587

# --- Content Configuration ---
# Path to the birthday image (relative to where the script runs)
IMAGE_PATH="./templates/birthday_image.jpg"
# Path to the HTML email template file
TEMPLATE_PATH="./templates/birthday_email.html"
# Company name for the email signature
COMPANY_NAME="Fischer Jordan"

# --- Google Cloud Service Account Credentials ---
# Paste the entire JSON content of your service account key file here,
# enclosed in single quotes and potentially needing escaping depending on your shell.
# OR set this environment variable directly in your deployment environment.
# Example (replace with your actual JSON):
# GOOGLE_CREDENTIALS='{"type": "service_account", "project_id": "...", ...}'
GOOGLE_CREDENTIALS='<PASTE YOUR SERVICE ACCOUNT JSON HERE>'